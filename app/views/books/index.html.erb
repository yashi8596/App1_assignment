<h1>Books</h1>

<p>title</p>
<% @books.each do |book| %>

  <p>題名:<%= book.title %></p>

<% end %>

<p>body</p>
<% @books.each do |book| %>

  <p>内容:<%= book.body %> <%= link_to 'Show', "/books/#{book.id}" %>  <%= link_to 'Edit', "/books/#{book.id}/edit" %>  <%= link_to 'Destroy', "/books/#{book.id}", method: :delete, "data-confirm" => "Are U Sure?" %></p>

<% end %>